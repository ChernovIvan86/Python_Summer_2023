                # Цикл while - "пока"
lst=[]                           # создан пустой список "lst"
zp=int(input("Введите ЗП:"))     # присвоить переменной zp целое значение, введённое с клавиатуры

if zp==0:                        # проверка деления на нуль
    while zp==0: zp=int(input\
    ("Перая ЗП должна быть  НЕ равна\
     НУЛЮ!!! Введите ЗП: "))     # проверка деления на нуль

lst.append(zp)                   # добавить значение содержащиеся в "zp" в конец списка "lst"
while int(zp) != 0:              # пока zp не равно 0 выполнять:
    zp=int(input("Введите ЗП:")) # присвоить переменной zp целое значение, введённое с клавиатуры
    lst.append(zp)               # добавить значение содержащиеся в "zp" в конец списка "lst"
    print("Введена ЗП:", lst)
#print(type(lst))
print(max(lst), min(lst), sum(lst))       # напеч max, min, sum из элементов списка "lst"
Mo_lst=int(sum(lst)) / (int(len(lst))-1)  # присвоить переменной Mo_lst
                                          # отношение суммы элементов списка "lst"
                                          # к числу его элементов
print("Средняя ЗП:", Mo_lst)